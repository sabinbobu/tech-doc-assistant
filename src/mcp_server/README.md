# MCP Server — Technical Documentation Assistant

MCP setup and Claude Desktop connection instructions now live in the project root's
[`README.md`](../../README.md#mcp-integration), under "MCP Integration" — this file used to
duplicate that content, which the project's own knowledge graph flagged as a documentation-drift
risk (`graphify-out/GRAPH_REPORT.md`, five `semantically_similar_to` edges between the two files).
